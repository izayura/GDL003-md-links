const path = require('path');

findMD = (filePath) =>{
  return path.extname(filePath) === '.md' ? true:false;

};

findLinks = () => {

};

printLinks = () => {

};

validateLinks = () => {

};

/* module.exports = (filePath) => {
  return path.extname(filePath) === '.md' ? true:false;

}; */

